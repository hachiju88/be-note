import ScreenPlaceholder from "@/components/ScreenPlaceholder";

export default function MenuPage() {
  return (
    <ScreenPlaceholder
      title="メニュー"
      url="/menu"
      role="staff / admin"
      description="各機能への入口。"
    />
  );
}
